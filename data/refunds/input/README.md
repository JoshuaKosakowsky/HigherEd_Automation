# Refund input downloads

Place the two manually downloaded Insights results here after renaming them:

- `refund_transactions.xlsx` or `.csv`
- `refund_context.xlsx` or `.csv`

The actual spreadsheet/CSV files are ignored by Git and must never be committed.
Delete or archive them according to the office's student-data retention process
after the refund review is complete.
